<!DOCTYPE html>
<html >

<head>
    <meta charset="utf-8">
</head>

<body>
    <section class="home">
    <h1>Passengers</h1>

    <% result.forEach( function (element) { %> 
                 <%= element.username %>  
                 <%= element.lat %>  
                 <%= element.lng %>  
    <% }); %>
    </section>
</body>

</html>
